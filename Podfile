# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Notebooks' do
  use_frameworks!
  pod 'SnapKit', '~> 4.0.0'
  pod 'SwiftIcons', '~> 2.2.0'

  target 'NotebooksTests' do
    inherit! :search_paths
  end

  target 'NotebooksUITests' do
    inherit! :search_paths
  end

end
